let arr =['John', 'Alice', 'Bob', 'Amanda']

for(x=0 ; x<arr.length ; x++){
    if(arr[x].includes('A')){
        let ans=arr.indexOf((arr[x]))

        console.log(ans);
        break;
        
    }
}

