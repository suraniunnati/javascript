// Write C Program to find gross salary by adding % of HRA, DA, and TA of user choice

let BaseSalary = 100,
  HRA = 10,
  DA = 5,
  TA = 8;

console.log("gross salary : ", BaseSalary + HRA + DA + TA);
